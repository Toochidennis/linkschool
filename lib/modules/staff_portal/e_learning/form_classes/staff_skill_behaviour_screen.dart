import 'package:flutter/material.dart';

class StaffSkillsBehaviourScreen extends StatelessWidget {
  const StaffSkillsBehaviourScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Skills and Behaviour'),
      ),
      body: const Center(child: Text('Skills and Behaviour Screen')),
    );
  }
}